# Unify Export Controls

## Changes
- Replace the separate Excel and PDF buttons on Accomplishments with one compact **Export** button that opens a format menu.
- Apply the same single-button export pattern to the Requests page for consistency.
- Preserve each page’s current filtered-data exports, loading state, disabled state, and success/error messages.
- Match the existing header Export menu styling so the control stays compact and responsive on phones.

## Verification
- Confirm the menu opens and both Excel and PDF choices remain available.
- Check Accomplishments and Requests at mobile and desktop widths.
- Confirm the project builds without errors.
